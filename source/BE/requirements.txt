fastapi>=0.95.0
uvicorn[standard]>=0.22.0
SQLAlchemy>=1.4
pymysql
cryptography
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
httpx>=0.24.0
google-auth>=2.22.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
requests>=2.31.0
python-dotenv>=1.0.0
pandas>=2.0.0
openpyxl>=3.1.0
pydantic[email]>=2.0.0
sse-starlette>=1.6.0
# ─── LLM & RAG (Member C) ───────────────────────────
# Gemini API — uses httpx (already included above)
chromadb>=0.4.0
sse-starlette>=1.6.0

# ─── Document Processing (Lightweight) ──────────────
aiofiles>=23.0.0
python-magic-bin>=0.4.14; sys_platform == 'win32'
pypdf>=3.0.0
python-docx>=1.0.0

# ─── Background Tasks ────────────────────────────────
celery>=5.3.0
redis>=5.0.0

# ─── Document Processing (Ingestion Pipeline) ───────
docling>=1.0.0
python-magic-bin>=0.4.14; sys_platform == 'win32'
aiofiles>=23.0.0

# ─── Advanced Document Processing ───────────────────
# OCR & Image Processing
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.16.3

# Advanced PDF Processing
pdfplumber>=0.10.0
PyMuPDF>=1.23.0  # fitz - for images, tables, metadata

# LlamaIndex for chunking
llama-index-core>=0.10.0
sentence-transformers>=2.2.0

# ─── New File Types Support (2026-03-29) ────────────
# PPTX (PowerPoint) support
python-pptx>=0.6.21

# XLSX (Excel) support - already included above (openpyxl>=3.1.0)

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=4.9.0

# RTF (Rich Text Format) support
striprtf>=0.0.26

# ODT (OpenDocument) support
odfpy>=1.4.1

# Note: CSV, JSON, Markdown use built-in Python modules (no extra deps)