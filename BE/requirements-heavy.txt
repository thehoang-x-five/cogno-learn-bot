# Cài trước — layer này được cache, không rebuild khi sửa code
# PyTorch CPU: cài trong Dockerfile (index-url riêng); không khai báo ở đây để tránh pip lấy bản CUDA từ PyPI.

# ─── Embedding & Vector DB ──────────────────────────
sentence-transformers>=2.2.0
chromadb>=0.4.0

# ─── Document Processing Pipeline ───────────────────
docling>=1.0.0
llama-index>=0.10.0
llama-index-core>=0.10.0
llama-index-embeddings-huggingface>=0.1.0

# ─── Additional Dependencies ─────────────────────────
tiktoken>=0.5.0
pypdf>=3.0.0
python-docx>=1.0.0