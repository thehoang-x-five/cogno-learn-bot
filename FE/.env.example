# Must match the FastAPI base URL your browser can reach (same origin policy + CORS).
# Do NOT use :8080 here — that port is for another project. Use http:// (not https) for local Docker backend.
VITE_API_URL=http://localhost:8000
VITE_FRONTEND_URL=http://localhost:3000
